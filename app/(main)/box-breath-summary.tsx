/**
 * Box-breath summary route — soft landing after a session.
 * Reached from /box-breath via End softly OR natural completion.
 * Hidden from tab bar.
 */
import SelfMindBoxBreathSummary from '@/components/practice/SelfMindBoxBreathSummary';
export default SelfMindBoxBreathSummary;
