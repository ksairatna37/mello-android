/**
 * Chat history route — full archive, reached from CHAT tab top-right.
 * Hidden from tab bar.
 */
import SelfMindChatHistory from '@/components/chat/SelfMindChatHistory';
export default SelfMindChatHistory;
