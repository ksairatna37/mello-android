/**
 * Chats route — SelfMind redesign chat home (CHAT tab landing).
 *
 * Renders SelfMindChatHome (thread index). Tapping a thread or "start
 * new thread" navigates to /chat where the legacy ChatScreen consumes
 * a chatNavStore request.
 */

import SelfMindChatHome from '@/components/chat/SelfMindChatHome';
export default SelfMindChatHome;
