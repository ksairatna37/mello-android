/**
 * Journal Entry route — single entry detail.
 *
 * Reached via `router.push('/journal-entry?id=<entryId>')` from
 * SelfMindJournalHome. Hidden from tab bar.
 */

import SelfMindJournalEntry from '@/components/journal/SelfMindJournalEntry';
export default SelfMindJournalEntry;
