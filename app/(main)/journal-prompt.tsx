/**
 * Journal Prompt route — guided composer.
 *
 * Reached from Home's "Journal prompt" card or from SelfMindJournalHome
 * compose action. Hidden from tab bar.
 */

import SelfMindJournalPrompt from '@/components/journal/SelfMindJournalPrompt';
export default SelfMindJournalPrompt;
