/**
 * Mood Detail route — single-dimension drilldown.
 *
 * Reached via `router.push('/mood-detail?dim=<key>')` from
 * SelfMindProgress's dimension cards. Hidden from tab bar.
 */

import SelfMindMoodDetail from '@/components/mood/SelfMindMoodDetail';
export default SelfMindMoodDetail;
