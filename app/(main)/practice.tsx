/**
 * Practice route — practices index ("a shelf of small, kind things").
 *
 * Reached from Home or Profile (TODO link). Hidden from tab bar.
 */

import SelfMindPracticeLibrary from '@/components/practice/SelfMindPracticeLibrary';
export default SelfMindPracticeLibrary;
