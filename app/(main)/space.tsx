/**
 * Space route — full-bleed sitting screen for a single Sound Space.
 *
 * Reached from /spaces by tapping a card. Receives `?id=<space-id>`. The
 * surface is immersive (no tab bar) — registered in FULL_SCREEN_ROUTES
 * in app/(main)/_layout.tsx.
 */

import SelfMindSoundSpaceSitting from '@/components/spaces/SelfMindSoundSpaceSitting';
export default SelfMindSoundSpaceSitting;
