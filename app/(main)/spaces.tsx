/**
 * Spaces route — Sound Spaces catalog ("where do you want to sit today?").
 *
 * Reached from Home (SoundSpacesCard). Tab bar visible (this is a hub
 * surface like /practice or /journal).
 */

import SelfMindSoundSpacesIndex from '@/components/spaces/SelfMindSoundSpacesIndex';
export default SelfMindSoundSpacesIndex;
